import React from 'react'

const AlertReducer = (state, action) => {
  switch (action.type) {
    case 'SHOW_ALERT':
      
      return
  
    default:
      return state
  }
}

export default AlertReducer